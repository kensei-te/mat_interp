# MySQL configuration section (host, user, password)
# Params
# host: ip address of mysql. for localservers specify localhost
# user: username (if any)
# password: password (if any)
[MySQL]
host = localhost
user = mat_user_1
password = mat_user_1_P